databaseChangeLog:
  - changeSet:
      id: "6"
      author: DimSelyutin
      changes:
        - createTable:
            tableName: user_subscription
            columns:
              - column:
                  name: id
                  type: UUID
                  defaultValueComputed: gen_random_uuid()
                  constraints:
                    primaryKey: true
                    nullable: false
              - column:
                  name: user_id
                  type: UUID
                  constraints:
                    nullable: false
                    foreignKeyName: fk_user_subscription_users
                    references: users(id)
                    deleteCascade: true
              - column:
                  name: subscription_id
                  type: UUID
                  constraints:
                    nullable: false
                    foreignKeyName: fk_user_subscription_subscription
                    references: subscription(id)
                    deleteCascade: true
              - column:
                  name: estimate
                  type: INT
                  constraints:
                    nullable: false
                    checkConstraint: "estimate > 0"
              - column:
                  name: start_date
                  type: TIMESTAMP
                  constraints:
                    nullable: false
              - column:
                  name: end_date
                  type: TIMESTAMP
                  constraints:
                    nullable: false
                    checkConstraint: "end_date > start_date"
              - column:
                  name: created_at
                  type: TIMESTAMP
                  defaultValueComputed: CURRENT_TIMESTAMP
                  constraints:
                    nullable: false
              - column:
                  name: updated_at
                  type: TIMESTAMP
                  defaultValueComputed: CURRENT_TIMESTAMP
                  constraints:
                    nullable: false
        - addUniqueConstraint:
            tableName: user_subscription
            columnNames: user_id, start_date, end_date
            constraintName: uq_user_subscription_user_start_end
      rollback:
        - dropTable:
            tableName: user_subscription